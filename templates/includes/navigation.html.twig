
<nav class="d-flex justify-content-between align-items-center px-lg-3 px-1">
		<div class="d-flex align-items-center">
		    <a href="{{path("app_admin")}}">
		    <img class="img-logo" src="{{ asset("img/logo.png") }}"/>
		    </a>
		</div>
		{% block additionalItem %}
		{% endblock %}
		<div>
			<button class="btn-grad grad-red full-center p-2 logout"  id="logout" data-target="#logout-modal">
				<svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" fill="currentColor" class="bi bi-power my-1" viewBox="0 0 16 16">
					<path d="M7.5 1v7h1V1h-1z"/>
					<path d="M3 8.812a4.999 4.999 0 0 1 2.578-4.375l-.485-.874A6 6 0 1 0 11 3.616l-.501.865A5 5 0 1 1 3 8.812z"/>
				</svg>
				<span class="d-none d-xl-inline ps-2">Déconnexion</span>
			</button>

		</div>
</nav>

{# Modal déconnexion #}
<div class="modal fade" id="logout-modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
	<div class="modal-dialog modal-dialog-centered" role="document">
		<div class="modal-content p-3">
			<div class="modal-body text-center">
			<div>
				<h4>
				Déconnexion
				</h4>
			</div>
			<div class="pt-2 pb-3">
				<p>Etes-vous sûr de vouloir vous déconnecter ?</p>
			</div>
			<div class="d-flex justify-content-around">
				<button type="button" class="btn-grad grad-green" data-dismiss="modal" id="modal-logout-close">Annuler</button>
				<a href="{{path("app_logout")}}"><button class="btn-grad grad-red">Confirmer</button></a>
			</div>
			</div>
		</div>
	</div>
</div>