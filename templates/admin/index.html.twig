{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{% for franchise in franchises %}
    <div>{{franchise.userInfo.name}}</div>
    {{franchise.userInfo.email}}
    <a href="{{path("app_edit_franchise", {'id': franchise.id })}}"><button>Editer</button></a>
{% endfor %}

<div>
<a href="{{path("app_create_franchise")}}"><button>Nouvelle franchise</button></a>
</div>


{% endblock %}
